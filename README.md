git init
git add .
git commit -m "Upload web-lab-2 index"
git branch -M main

# УВАГА: ЗАМІНІТЬ "ВАШ_НІК" на ваш реальний логін на GitHub!
git remote add origin https://github.com/nann72/web-lab-2.git

git push -u origin main
